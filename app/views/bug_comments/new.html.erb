<h1>New Comment</h1>

<%= form_for(@bug_comment) do |f| %>
  <% if @bug_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @bug_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :rows => 6 %>
  </div>

  <div class="actions">
    <%= f.submit :value => 'Submit Comment'%>
  </div>
<% end %>

<%= link_to 'Back', bugs_path %>
